# Copyright 2026 VMware, Inc.
# SPDX-License-Identifier: BSD-2-Clause
---
# Release DHCP IP address in guest OS
- name: "Set fact of the shell command to release DHCP IP address in Linux guest OS"
  ansible.builtin.set_fact:
    vm_release_dhcp_ip_cmd: >-
      {% if guest_os_network_manager | default('') == 'NetworkManager' %}
        iface=$(ip -o -4 route show to default | head -n 1 | awk '{print $5}');
        conn=$(nmcli -t -f NAME,DEVICE connection show --active | grep ":$iface$" | head -n 1 | cut -d: -f1);
        nmcli connection modify "$conn" ipv4.dhcp-send-release yes;
        nmcli connection up "$conn";
        nmcli connection down "$conn"
      {% elif guest_os_network_manager | default('') == 'wicked' %}
        wicked ifdown --release $(ip route show default | awk '{print $5}')
      {% else %}
        dhclient -r eth0
      {% endif %}
  when: gosv_test_suite == 'linux'

- name: "Execute command to release DHCP IP address in guest OS"
  when: vm_guest_ip is defined and vm_guest_ip
  block:
    - name: "Release DHCP IP address in Windows guest OS"
      ansible.windows.win_shell: "ipconfig /release"
      delegate_to: "{{ vm_guest_ip }}"
      ignore_errors: true
      ignore_unreachable: true
      when: gosv_test_suite == 'windows'

    - name: "Release DHCP IP address in Linux guest OS via SSH"
      ansible.builtin.shell: "{{ vm_release_dhcp_ip_cmd }}"
      delegate_to: "{{ vm_guest_ip }}"
      ignore_errors: true
      ignore_unreachable: true
      when: gosv_test_suite == 'linux'

- name: "Get VMware Tools status in guest OS"
  include_tasks: vm_get_vmtools_status.yml

- name: "Shutdown Linux guest OS via VMware Tools"
  vmware.vmware.vm_powerstate:
    validate_certs: "{{ validate_certs | default(false) }}"
    datacenter: "{{ vsphere_host_datacenter }}"
    hostname: "{{ vsphere_host_name }}"
    username: "{{ vsphere_host_user }}"
    password: "{{ vsphere_host_user_password }}"
    folder: "{{ vm_folder }}"
    name: "{{ vm_name }}"
    state: "shutdown-guest"
    timeout: 300
    force: true
  ignore_errors: true
  when: vmtools_is_running