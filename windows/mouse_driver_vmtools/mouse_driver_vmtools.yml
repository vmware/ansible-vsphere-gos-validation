# Copyright 2021-2026 VMware, Inc.
# SPDX-License-Identifier: BSD-2-Clause
---
# Description:
#   This test case is used for check mouse driver after VMware Tools
# install. The test result will be 'No Run' if VMware Tools is not
# installed or not running.
#
- name: mouse_driver_vmtools
  hosts: localhost
  gather_facts: false
  tasks:
    - name: "Test case block"
      block:
        - name: "Test setup"
          include_tasks: ../setup/test_setup.yml
          vars:
            skip_test_no_vmtools: true

        - name: "Initialize the device name and driver version"
          ansible.builtin.set_fact:
            vmware_pointing_device_driver: ""
            vmware_pointing_device_name: "{{ (esxi_cpu_vendor == 'arm') | ternary('VMware USB Pointing Device', 'VMware Pointing Device') }}"

        - name: "Get pointing device driver version in Windows guest OS"
          include_tasks: ../utils/win_execute_cmd.yml
          vars:
            win_powershell_cmd: "(Get-WmiObject Win32_PnpSignedDriver | where-object {$_.DeviceName -Match '{{ vmware_pointing_device_name }}'}).DriverVersion"
            win_execute_cmd_ignore_error: true

        - name: "Set fact of the VMware pointing device driver version"
          ansible.builtin.set_fact:
            vmware_pointing_device_driver: "{{ win_powershell_cmd_output.stdout_lines[0] }}"
          when:
            - win_powershell_cmd_output.stdout_lines is defined
            - win_powershell_cmd_output.stdout_lines | length == 1

        - name: "Print the VMware pointing device driver version"
          ansible.builtin.debug: var=vmware_pointing_device_driver

        - name: "Verify VMware pointing device driver version"
          ansible.builtin.assert:
            that:
              - vmware_pointing_device_driver | length != 0
              - vmware_pointing_device_driver is regex("^\d+\.\d+\.\d+\.\d+$")
            fail_msg: >-
              Failed to get '{{ vmware_pointing_device_name }}' driver version due to '{{ win_powershell_cmd_output.msg | default('') }}',
              or the driver version '{{ vmware_pointing_device_name }}' is not in the expected format as '1.2.3.4'.
            success_msg: "Get '{{ vmware_pointing_device_name }}' driver version '{{ vmware_pointing_device_driver }}'."
      rescue:
        - name: "Test case failure"
          include_tasks: ../../common/test_rescue.yml
