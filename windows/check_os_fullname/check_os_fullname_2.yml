# ESXi version >= win_guest_add_esxi (or win_guest_creation_esxi)
# 1. Installed VMware Tools >= win_guest_vmtools
# > expected guest info
 
# 2. Installed VMware Tools < win_guest_vmtools
# > configured guest ID should be the expected guest ID
# > guest ID corresponding guest info

# ESXi version < win_guest_add_esxi (or win_guest_creation_esxi)
# 1. Installed VMware Tools >= win_guest_vmtools
# > configured guest ID should be the previous guest ID
# > guest ID corresponding guest info
 
# 2. Installed VMware Tools < win_guest_vmtools
# > configured guest ID should be the previous guest ID
# > guest ID corresponding guest info
